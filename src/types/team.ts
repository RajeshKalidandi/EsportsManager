export interface Player {
  _id: string;
  name: string;
  position: string;
  jerseyNumber: string;
}

export interface Team {
  _id: string;
  name: string;
  // Add other properties of the Team model here
}
